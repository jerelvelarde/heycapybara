# Set secrets in the process environment (e.g. op run). Never commit real keys.
OPENAI_API_KEY=
CPK_INTELLIGENCE_API_KEY=
# Create this container in your Intelligence project first.
CPK_INTELLIGENCE_LEARNING_CONTAINER_ID=desktop-workflows
KITE_MODEL=gpt-5.4
